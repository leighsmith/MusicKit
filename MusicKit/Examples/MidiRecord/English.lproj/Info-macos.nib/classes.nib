{
    IBClasses = (
        {
            ACTIONS = {orderFrontNXColorPanel = id; }; 
            CLASS = FirstResponder; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }, 
        {
            ACTIONS = {go = id; save = id; saveAs = id; showInfoPanel = id; terminate = id; }; 
            CLASS = MidiRecord; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                infoPanel = id; 
                myWindow = id; 
                saveAsMenuItem = id; 
                saveMenuItem = id; 
                score = id; 
                scoreRecorder = id; 
                stringTable = id; 
                theButton = id; 
            }; 
            SUPERCLASS = NSApplication; 
        }, 
        {
            ACTIONS = {go = id; save = id; saveAs = id; showInfoPanel = id; terminate = id; }; 
            CLASS = MyApp; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                infoPanel = id; 
                myWindow = id; 
                saveAsMenuItem = id; 
                saveMenuItem = id; 
                stringTable = id; 
                theButton = id; 
            }; 
            SUPERCLASS = NSApplication; 
        }
    ); 
    IBVersion = 1; 
}