{
    IBClasses = (
        {
            ACTIONS = {orderFrontNXColorPanel = id; }; 
            CLASS = FirstResponder; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }, 
        {
            ACTIONS = {go = id; save = id; saveAs = id; setDriverName = id; showInfoPanel = id; }; 
            CLASS = MidiRecord; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                driverPopup = id; 
                infoPanel = id; 
                myWindow = id; 
                recordButton = id; 
                saveAsMenuItem = id; 
                saveMenuItem = id; 
            }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}