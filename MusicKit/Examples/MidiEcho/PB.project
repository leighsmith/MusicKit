{
    APPCLASS = NSApplication; 
    FILESTABLE = {
        CLASSES = (EchoFilter.m, MidiEcho.m); 
        C_FILES = (); 
        English_INTERFACES = (MidiEcho.nib, Info.nib); 
        FRAMEWORKS = (
            AppKit.framework, 
            Foundation.framework, 
            MusicKit.framework, 
            SoundKit.framework
        ); 
        FRAMEWORKSEARCH = (/Local/Users/leigh/Library/Frameworks, /System/Library/PrivateFrameworks); 
        H_FILES = (EchoFilter.h, MidiEcho.h); 
        IMAGES = (MidiEcho.tiff); 
        INTERFACES = (); 
        M_FILES = (); 
        OTHER_LIBS = (); 
        OTHER_LINKED = (MidiEcho_main.m); 
        OTHER_SOURCES = (Makefile, MidiEcho.iconheader, README); 
        PRECOMPILED_HEADERS = (); 
        PROJECT_HEADERS = (); 
        PSWM_FILES = (); 
        PSW_FILES = (); 
        PUBLIC_HEADERS = (); 
        SOUNDS = (); 
        SUBPROJECTS = (); 
    }; 
    GENERATEMAIN = YES; 
    LANGUAGE = English; 
    MAKEFILEDIR = "$(MAKEFILEPATH)/pb_makefiles"; 
    NEXTSTEP_APPICON = MidiEcho.tiff; 
    NEXTSTEP_BUILDTOOL = /bin/gnumake; 
    NEXTSTEP_DOCUMENTEXTENSIONS = (); 
    NEXTSTEP_INSTALLDIR = "$(HOME)/Apps"; 
    NEXTSTEP_JAVA_COMPILER = /usr/bin/javac; 
    NEXTSTEP_MAINNIB = MidiEcho; 
    NEXTSTEP_OBJCPLUS_COMPILER = /usr/bin/cc; 
    PDO_UNIX_INSTALLDIR = "$(HOME)/Apps"; 
    PDO_UNIX_JAVA_COMPILER = "$(NEXTDEV_BIN)/javac"; 
    PDO_UNIX_MAINNIB = MidiEcho; 
    PDO_UNIX_OBJCPLUS_COMPILER = "$(NEXTDEV_BIN)/gcc"; 
    PROJECTNAME = MidiEcho; 
    PROJECTTYPE = Application; 
    PROJECTVERSION = 2.8; 
    SYSTEMEXTENSIONS = (); 
    WINDOWS_INSTALLDIR = "$(HOME)/Apps"; 
    WINDOWS_JAVA_COMPILER = "$(JDKBINDIR)/javac.exe"; 
    WINDOWS_MAINNIB = MidiEcho; 
    WINDOWS_OBJCPLUS_COMPILER = "$(DEVDIR)/gcc"; 
}
