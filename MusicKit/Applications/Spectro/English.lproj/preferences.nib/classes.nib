{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {defaults = id; okay = id; setPref = id; }; 
            CLASS = PrefController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                amplitudeColorWell = NSColorWell; 
                colorView = id; 
                cursorColorWell = NSColorWell; 
                dBLimitCell = id; 
                displayMode = id; 
                fftView = id; 
                gridColorWell = NSColorWell; 
                hopRatioCell = id; 
                multiView = id; 
                soundDisplayView = id; 
                spectrumColorWell = NSColorWell; 
                spectrumDisplayView = id; 
                spectrumMaxFreqCell = id; 
                waterfallColorWell = NSColorWell; 
                wfMaxFreqCell = id; 
                wfPlotHeightCell = id; 
                window = id; 
                windowSizeCell = id; 
                windowTypeMatrix = id; 
                zpFactorCell = id; 
            }; 
            SUPERCLASS = NSObject; 
        }, 
        {
            ACTIONS = {
                appDidHide = id; 
                appDidInit = id; 
                appWillTerminate = id; 
                newSoundDoc = id; 
                open = id; 
                revertToSaved = id; 
                save = id; 
                saveAs = id; 
                saveTo = id; 
                showInfoPanel = id; 
                showPreferences = id; 
                sndInfo = id; 
            }; 
            CLASS = SoundController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                currentDocument = id; 
                documentList = id; 
                infoPanel = id; 
                prefController = id; 
                saveToAccessoryView = id; 
                saveToController = id; 
                stringTable = id; 
            }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}