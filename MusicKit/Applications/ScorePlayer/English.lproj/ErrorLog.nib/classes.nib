{
    IBClasses = (
        {
            ACTIONS = {buttonPressed = id; }; 
            CLASS = ErrorLog; 
            LANGUAGE = ObjC; 
            OUTLETS = {msg = id; panel = id; }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}