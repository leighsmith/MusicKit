#
# Generated by the NeXT Project Builder.
#
# NOTE: Do NOT change this file -- Project Builder maintains it.
#
# Put all of your customizations in files called Makefile.preamble
# and Makefile.postamble (both optional), and Makefile will include them.
#

NAME = SlideFlute

PROJECTVERSION = 2.8
PROJECT_TYPE = Application
NEXTSTEP_APPICON = Fluter.tiff
ICONSECTIONS =	-sectcreate __ICON app Fluter.tiff

English_RESOURCES = SlideFlute.nib

LANGUAGES = English

GLOBAL_RESOURCES = FluteHead.tiff Fluter.tiff README.txt

CLASSES = Controller.m FluteIns.m MidiController.m

HFILES = Controller.h FluteIns.h MidiController.h

MFILES = SlideFlute_main.m

OTHERSRCS = Makefile.preamble Makefile Makefile.postamble


MAKEFILEDIR = /System/Developer/Makefiles/pb_makefiles
CODE_GEN_STYLE = DYNAMIC
MAKEFILE = app.make
NEXTSTEP_INSTALLDIR = $(HOME)/Apps
WINDOWS_INSTALLDIR = $(HOME)/Apps
PDO_UNIX_INSTALLDIR = $(HOME)/Apps
LIBS = -lsynthpatches -lunitgenerators -lmusickit -ldsp -lMedia_s\
       -lNeXT_s -lDriver
DEBUG_LIBS = $(LIBS)
PROF_LIBS = $(LIBS)


FRAMEWORK_PATHS = -F/Local/Library/Frameworks
FRAMEWORKS = -framework AppKit -framework dsp -framework Foundation\
             -framework MKPerformSndMIDI -framework MusicKit


NEXTSTEP_OBJCPLUS_COMPILER = /usr/bin/cc
WINDOWS_OBJCPLUS_COMPILER = $(DEVDIR)/gcc
PDO_UNIX_OBJCPLUS_COMPILER = $(NEXTDEV_BIN)/gcc
NEXTSTEP_JAVA_COMPILER = /usr/bin/javac
WINDOWS_JAVA_COMPILER = $(JDKBINDIR)/javac.exe
PDO_UNIX_JAVA_COMPILER = $(JDKBINDIR)/javac

include $(MAKEFILEDIR)/platform.make

-include Makefile.preamble

include $(MAKEFILEDIR)/$(MAKEFILE)

-include Makefile.postamble

-include Makefile.dependencies
