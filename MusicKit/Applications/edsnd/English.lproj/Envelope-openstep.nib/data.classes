Envelope = {
    ACTIONS = {"doEnvelope:" = "doEnvelope:"; "envelopeChanged:" = "envelopeChanged:"; }; 
    OUTLETS = {
	envPanel = envPanel; 
	envText = envText; 
	envView = envView; 
	pointForm = pointForm; 
	xForm = xForm; 
	yForm = yForm; 
    }; 
    SUPERCLASS = Object; 
};
FirstResponder = {ACTIONS = {}; SUPERCLASS = Object; };
EnvelopeView = {SUPERCLASS = View; };
UpPanel = {SUPERCLASS = Panel; };
